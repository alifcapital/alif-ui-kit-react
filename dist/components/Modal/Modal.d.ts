import React from 'react';
import { IModalProps } from './ModalTypes';
export declare const Modal: ({ open, onOpenChange, children, disableBackdropClick, disableEscapeKey, }: IModalProps) => React.JSX.Element | null;
