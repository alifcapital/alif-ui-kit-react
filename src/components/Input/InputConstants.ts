export enum INPUT_TYPE {
  Text = 'text',
  Number = 'number',
  Password = 'password',
  Telephone = 'tel',
  Email = 'email',
}

export enum INPUT_SIZE {
  Small = 'small',
  Medium = 'medium',
}

export enum INPUT_THEME {
  Light = 'light',
  Dark = 'dark',
}
