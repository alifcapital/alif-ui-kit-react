export enum InputType {
  Text = 'text',
  Number = 'number',
  Password = 'password',
}

export enum InputSize {
  Small = 'small',
  Medium = 'medium',
}

export enum InputTheme {
  Light = 'light',
  Dark = 'dark',
}
